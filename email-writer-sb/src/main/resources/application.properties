spring.application.name=email-writer-sb

# Bind to the port the host assigns (Render sets $PORT); default 8080 locally.
server.port=${PORT:8080}

gemeni.api.url=${GEMINI_URL}
gemini.api.key=${GEMINI_KEY}
